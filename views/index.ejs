<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hokie Wheels Rentals</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="style.css" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
  </head>
  <body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg header navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="#">Hokie Wheels Rentals</a>
        <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <a class="nav-link" href="#">Login</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Sign Up</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Hero Image with Parallax Effect -->
    <div class="hero-image">
      <div class="hero-text">
        <h1>Welcome to Hokie Wheels Rentals</h1>
        <p>Find the perfect ride for your journey</p>
      </div>
    </div>
    <div class="container">
      <div class="row">
        <div class="col-md-8 offset-md-2">
          <!-- Search Bar -->
          <div class="container search-field">
            <div class="row">
              <div class="col-md-6 offset-md-3 text-center">
                <h1>Reserve a Vehicle</h1>
                <h4 id="locationLabel">
                  Pick-up & Return Location (ZIP, City, or Airport)*
                </h4>
                <form id="reservationForm">
                  <div class="form-group" id="locationGroup">
                    <input
                      type="text"
                      class="form-control"
                      id="locationInput"
                      placeholder="Enter Location"
                      autocomplete="off"
                    />
                    <div
                      id="locationSuggestions"
                      class="suggestions-dropdown"
                    ></div>
                  </div>
                  <div id="selectedLocationDisplay" style="display: none">
                    <span id="selectedLocationName"></span>
                    <button id="editLocationBtn">Edit Location</button>
                  </div>
                  <div
                    class="form-group"
                    id="pickupGroup"
                    style="display: none"
                  >
                    <label for="pickupDate">Pick-up Date & Time*</label>
                    <input
                      type="datetime-local"
                      class="form-control"
                      id="pickupDate"
                    />
                  </div>
                  <div
                    class="form-group"
                    id="dropoffGroup"
                    style="display: none"
                  >
                    <label for="dropoffDate">Drop-off Date & Time*</label>
                    <input
                      type="datetime-local"
                      class="form-control"
                      id="dropoffDate"
                    />
                  </div>
                  <button
                    type="submit"
                    class="btn btn-primary"
                    id="submitButton"
                    style="display: none"
                  >
                    Search
                  </button>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="test marketing">
        <div class="row featurette">
          <div class="col-lg-4">
            <div class="featurette-item">
              <img class="featurette-icon" src="images/1.jpg" alt="Icon 1" />
              <h2 class="featurette-heading">Current Deals</h2>
              <p class="lead">
                Explore our current deals and promotions or start a reservation
                to find the right vehicle at everyday low rates.
              </p>
              <a class="btn btn-secondary" href="#">Learn More</a>
            </div>
          </div>
          <div class="col-lg-4">
            <div class="featurette-item">
              <img class="featurette-icon" src="images/2.jpg" alt="Icon 2" />
              <h2 class="featurette-heading">Your Kind of Travel Planning</h2>
              <p class="lead">
                Sign up to receive our latest benefits, updates and great rates
                delivered to your inbox.
              </p>
              <a class="btn btn-secondary" href="#">Sign Up</a>
            </div>
          </div>
          <div class="col-lg-4">
            <div class="featurette-item">
              <img class="featurette-icon" src="images/3.jpg" alt="Icon 3" />
              <h2 class="featurette-heading">Hokie Plus® Program</h2>
              <p class="lead">
                You could be earning points toward free rentals. What are you
                waiting for?
              </p>
              <a class="btn btn-secondary" href="#">Join Now</a>
            </div>
          </div>
        </div>

        <hr class="featurette-divider" />

        <div class="row">
          <!-- Dynamically insert vehicle items here -->
        </div>
      </div>
    </div>

    <!-- Bootstrap JS and jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        const locationInput = document.getElementById("locationInput");
        const suggestionsContainer = document.getElementById(
          "locationSuggestions"
        );
        const selectedLocationDisplay = document.getElementById(
          "selectedLocationDisplay"
        );
        const selectedLocationName = document.getElementById(
          "selectedLocationName"
        );
        const editLocationBtn = document.getElementById("editLocationBtn");
        const pickupGroup = document.getElementById("pickupGroup");

        locationInput.addEventListener("input", function () {
          const inputVal = this.value;
          if (inputVal.length > 1) {
            // Fetch suggestions if input length is more than 1 character
            fetchSuggestions(inputVal);
          } else {
            suggestionsContainer.innerHTML = "";
          }
        });

        function fetchSuggestions(input) {
          // Replace with the actual API endpoint
          fetch(`/locations?q=${encodeURIComponent(input)}`)
            .then((response) => response.json())
            .then((locations) => {
              displaySuggestions(locations);
            })
            .catch((error) =>
              console.error("Error fetching location suggestions:", error)
            );
        }

        function displaySuggestions(locations) {
          suggestionsContainer.innerHTML = "";
          if (locations.length) {
            locations.forEach((location) => {
              const div = document.createElement("div");
              div.textContent = location.city; // Adjust according to your actual property name
              div.style.padding = "10px";
              div.style.cursor = "pointer";
              div.addEventListener("click", function () {
                locationInput.value = this.textContent;
                suggestionsContainer.innerHTML = ""; // Clear suggestions after selection
                locationInput.value = "";
                locationInput.style.display = "none"; // Hide input field
                selectedLocationName.textContent = this.textContent;
                selectedLocationDisplay.style.display = "block"; // Show selected location and edit option
                suggestionsContainer.innerHTML = ""; // Clear suggestions
                pickupGroup.style.display = "block"; // Show pickup date and time
              });
              suggestionsContainer.appendChild(div);
            });
          } else {
            suggestionsContainer.innerHTML =
              '<div style="padding: 10px;">No matches found</div>';
          }
        }
        // Handler for edit location button
        editLocationBtn.addEventListener("click", function () {
          selectedLocationDisplay.style.display = "none"; // Hide selected location display
          locationInput.style.display = "block"; // Show location input field again
          locationInput.focus(); // Optionally, focus the input field
          pickupGroup.style.display = "none"; // Optionally, hide the pickup group until a location is selected again
        });
      });
    </script>
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        // Existing variables
        const locationGroup = document.getElementById("locationGroup");
        const dropoffGroup = document.getElementById("dropoffGroup");
        const submitButton = document.getElementById("submitButton");
        const locationInput = document.getElementById("locationInput");
        const locationLabel = document.getElementById("locationLabel");
        const pickupGroup = document.getElementById("pickupGroup");

        // Modify this part: listen for the 'keydown' event instead of 'blur'
        locationInput.addEventListener("keydown", function (event) {
          // Check if the pressed key is 'Enter'
          if (event.key === "Enter") {
            event.preventDefault(); // Prevent form submission if inside a form
            if (locationInput.value.trim() !== "") {
              // Show pickup and dropoff sections
              pickupGroup.style.display = "block";
              dropoffGroup.style.display = "block";
              submitButton.style.display = "block";

              // Optionally, you might want to move focus to the next input field for a smoother user experience
              document.getElementById("pickupDate").focus();

              // Hide location section
              locationGroup.style.display = "none";
              locationLabel.style.display = "none";
            }
          }
        });
      });
    </script>
  </body>
</html>
